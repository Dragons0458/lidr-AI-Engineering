[project]
name = "estimador-cag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "httpx",
    "jinja2>=3.1.6",
    "litellm",
    "litellm[google]",
    "pydantic-settings",
    "python-dotenv",
    "ruff",
    "streamlit>=1.57.0",
    "structlog",
    "uvicorn[standard]",
    "watchdog>=6.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
